# Frontend environment example

# 1) 同一オリジンでリバースプロキシする（推奨・ヒューマンエラーが少ない）
#    Webサーバー側で /api と /uploads をバックエンドへプロキシします。
#    その場合は BASE を空のままにして、fetch は同一オリジンに飛ばします。
NEXT_PUBLIC_API_BASE=

# 2) 直接APIへクロスオリジンでアクセスする場合（使うなら変更してください）
# NEXT_PUBLIC_API_BASE=http://toybox.ayatori-inc.co.jp
# NEXT_PUBLIC_API_BASE=https://api.toybox.ayatori-inc.co.jp

# SSR/ビルド時にバックエンドへアクセスするための内部URL（任意）
# 未設定なら Next のリライトは無効になり、外部プロキシに委ねます。
# BACKEND_INTERNAL_URL=http://backend:4000

# Web Vitals の送信（既定: 無効）
NEXT_PUBLIC_ENABLE_WEBVITALS=

# サイトのフルURL（OGPなどで使用する場合に設定）
# NEXT_PUBLIC_SITE_URL=https://toybox.example.com

# For Vercel (set in Project Settings)
# NEXT_PUBLIC_API_BASE=https://your-backend.example.com